function square(x){
    sq= x*x;           // or just write--> return (x*x); in the function body.
    return sq;
}
let nomber= square(4);
console.log(nomber);

//In-built function: example--> Math.pow(x,y)
let b=Math.pow(3,4);
console.log(b);
